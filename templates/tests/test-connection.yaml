apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datav1c.fullname" . }}-test-connection"
  labels:
    {{- include "datav1c.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datav1c.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
